import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs';
import { CustomButton } from '@components/customButton';

<Meta 
  title="documentacion/CustomButton" 
  component={CustomButton} 
  parameters={{ 
    docs: {
      description: {
        component: 'Este es un botón personalizado que se puede usar en varias partes de la aplicación.',
      },
    },
  }}
/>

# CustomButton

Este es un botón personalizado que se puede usar en varias partes de la aplicación.

## Props

<ArgsTable of={CustomButton} />

## Ejemplos

### Ejemplo de Uso

```jsx
<Canvas>
  <Story name="Button">
    {(args) => <CustomButton {...args} />}
  </Story>
</Canvas>
